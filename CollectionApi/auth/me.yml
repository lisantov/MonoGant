info:
  name: me
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}{{prefix}}/user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false