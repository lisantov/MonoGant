info:
  name: change password
  type: http
  seq: 5

http:
  method: POST
  url: "{{host}}{{prefix}}/change-password"
  body:
    type: json
    data: |-
      {
        "old_password": "QWEasd123!!!",
        "new_password": "qweASD123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
