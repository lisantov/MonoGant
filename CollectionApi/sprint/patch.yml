info:
  name: patch
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{host}}{{prefix}}/sprints/1"
  body:
    type: json
    data: |-
      {
        "name": "sprint2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
