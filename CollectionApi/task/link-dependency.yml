info:
  name: link-dependency
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}{{prefix}}/tasks/1/dependencies"
  body:
    type: json
    data: |-
      {
        "predecessor_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false