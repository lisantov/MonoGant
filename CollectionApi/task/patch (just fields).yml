info:
  name: patch (just fields)
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}{{prefix}}/tasks/2"
  body:
    type: json
    data: |-
      {
        "name": "task2",
        "started_at": "2026-09-18",
        "deadline_at": "2026-09-20"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
