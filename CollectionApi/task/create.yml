info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}{{prefix}}/sprints/1/tasks"
  body:
    type: json
    data: |-
      {
        "name": "task",
        "description": "task description",
        "started_at": "2026-09-12",
        "deadline_at": "2026-09-14",
        "user_email": "email@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
