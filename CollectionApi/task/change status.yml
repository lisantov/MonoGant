info:
  name: change status
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{host}}{{prefix}}/tasks/2"
  body:
    type: json
    data: |-
      {
        "status": "done"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
