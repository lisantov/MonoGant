info:
  name: delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}{{prefix}}/tasks/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
