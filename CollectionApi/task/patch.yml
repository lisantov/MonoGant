info:
  name: patch
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}{{prefix}}/tasks/1"
  body:
    type: json
    data: |-
      {
        "name": "task2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false