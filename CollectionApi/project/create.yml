info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}{{prefix}}/projects"
  body:
    type: json
    data: |-
      {
        "name": "project",
        "started_at": "2026-09-10",
        "deadline_at": "2026-09-11"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
