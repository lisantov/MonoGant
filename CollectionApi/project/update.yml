info:
  name: update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{host}}{{prefix}}/projects/1"
  body:
    type: json
    data: |-
      {
        "name": "arche"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
