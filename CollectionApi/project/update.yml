info:
  name: update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{host}}{{prefix}}/projects/1"
  body:
    type: json
    data: |-
      {
        "name": "arche",
        "deadline_at": "2026-09-15",
        "status": "in_progress"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
