info:
  name: remove-member
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{host}}{{prefix}}/projects/1/members/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false