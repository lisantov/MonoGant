info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}{{prefix}}/tasks/1/comments"
  body:
    type: json
    data: |-
      {
        "body": "comment body"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false