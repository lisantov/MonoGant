info:
  name: index
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}{{prefix}}/tasks/1/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false